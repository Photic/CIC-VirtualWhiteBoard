export class API {

    // Just nice to have API endpoints in one place.

    // sql //
    static get_posts = '/api/v1/grid/get'
    static get_users_team = '/api/v1/user/get/team'

    // Auth //
    static post_auth = '/api/v1/auth';
}